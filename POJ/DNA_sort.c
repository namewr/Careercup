#include<stdio.h>

static inline void ins_best(int *arr, int num)
{
}


int main()
{
	int arr[256], idx[256], num, size;
	int i;
	char str[256][256], tmp[256];

	scanf("%d %d", &num, &size);
	for(i = 0; i < num; i++){
		scanf("%s", str[i]);
	}

	memcpy(str[i], tmp, size);
	
}
